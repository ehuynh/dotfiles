bind generic 0 @sh -c "echo %(commit) | cut -c 1-7 | pbcopy" # copy commit hash to clipboard (truncated to 7 chars)
bind stash A !?git stash apply %(stash)
set vertical-split = no
set split-view-height = 90%

